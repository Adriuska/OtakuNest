<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}OtakuNest{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="icon" href="https://fav.farm/🌀">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    {% block stylesheets %}{% endblock %}
    <style>
        /* FONDO OSCURO Y VIBRANT */
        body {
            background: linear-gradient(135deg, #0f172a 0%, #1a1f3a 50%, #16213e 100%) !important;
            background-attachment: fixed !important;
            color: #f1f5f9 !important;
            min-height: 100vh !important;
            display: flex !important;
            flex-direction: column !important;
        }

        main {
            flex: 1;
        }

        /* GLASS CARDS */
        .glass-card {
            background: rgba(255, 255, 255, 0.05) !important;
            backdrop-filter: blur(10px) !important;
            border: 1px solid rgba(255, 255, 255, 0.1) !important;
            border-radius: 16px !important;
        }

        /* INPUTS OSCUROS */
        .form-control, .form-select {
            background: rgba(255, 255, 255, 0.08) !important;
            border: 1px solid rgba(255, 255, 255, 0.15) !important;
            color: #f1f5f9 !important;
        }

        /* SELECTS Y INPUTS OSCUROS */
        .form-control-dark,
        .form-select-dark {
            background: rgba(30, 30, 50, 0.8) !important;
            border: 2px solid rgba(124, 58, 237, 0.3) !important;
            color: #f1f5f9 !important;
            font-weight: 500 !important;
        }

        .form-control-dark:hover,
        .form-select-dark:hover {
            background: rgba(40, 40, 60, 0.9) !important;
            border-color: rgba(124, 58, 237, 0.5) !important;
        }

        /* OPCIONES DEL SELECT */
        select option {
            background: #1a1a2e !important;
            color: #f1f5f9 !important;
        }

        select option:checked {
            background: #7c3aed !important;
            color: #fff !important;
        }

        .form-control:focus, .form-select:focus {
            background: rgba(124, 58, 237, 0.1) !important;
            border-color: #7c3aed !important;
            color: #f1f5f9 !important;
            box-shadow: 0 0 0 0.2rem rgba(124, 58, 237, 0.25) !important;
        }

        /* BOTONES CON GRADIENTE */
        .btn-warning {
            background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%) !important;
            border: none !important;
            color: white !important;
            font-weight: 600 !important;
        }

        .btn-warning:hover {
            background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%) !important;
            color: white !important;
        }

        /* TODOS LOS BOTONES CON TEXTO BLANCO */
        .btn {
            color: white !important;
            font-weight: 500 !important;
        }

        .btn-outline-light {
            color: white !important;
            border-color: rgba(255, 255, 255, 0.3) !important;
        }

        .btn-outline-light:hover {
            color: white !important;
            background-color: rgba(255, 255, 255, 0.1) !important;
        }

        .btn-danger {
            color: white !important;
        }

        .btn-danger:hover {
            color: white !important;
        }

        .btn-success {
            color: white !important;
        }

        .btn-info {
            color: white !important;
        }

        .list-group-item {
            color: #f1f5f9 !important;
        }

        .list-group-item:hover {
            color: white !important;
        }

        /* LABELS Y TEXTO */
        .form-label, label {
            color: #cbd5e1 !important;
            font-weight: 500 !important;
        }

        /* LINKS NARANJA */
        .text-warning {
            color: #f59e0b !important;
        }

        a {
            color: #7c3aed !important;
            text-decoration: none !important;
        }

        a:hover {
            color: #a78bfa !important;
        }

        /* HEADINGS CLAROS */
        h1, h2, h3, h4, h5, h6 {
            color: #f1f5f9 !important;
        }

        /* NAVBAR */
        .navbar {
            background: rgba(15, 23, 42, 0.95) !important;
        }

        .nav-link {
            color: #cbd5e1 !important;
        }

        .nav-link:hover {
            color: #a78bfa !important;
        }

        /* FOOTER */
        footer {
            background: rgba(15, 23, 42, 0.95) !important;
            color: #f1f5f9 !important;
        }

        .text-white {
            color: #f1f5f9 !important;
        }

        /* CAMBIAR TODOS LOS TEXTOS GRISES A BLANCO */
        .text-muted {
            color: #cbd5e1 !important;
        }

        /* OPTIMIZAR IMÁGENES */
        img {
            image-rendering: crisp-edges !important;
            -ms-interpolation-mode: nearest-neighbor !important;
        }

        img[loading="lazy"] {
            object-fit: cover !important;
            object-position: center !important;
        }
    </style>
</head>
<body>
    <header class="py-3">
        <div class="container">
            <nav class="navbar navbar-expand-lg navbar-dark glass-card px-4 py-3">
                <a class="navbar-brand d-flex align-items-center gap-2" href="{{ path('app_home') }}">
                    <i class="fa-solid fa-meteor fa-lg text-warning"></i>
                    <span>OtakuNest</span>
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="mainNav">
                    <ul class="navbar-nav ms-auto gap-lg-3 text-uppercase small fw-semibold">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_home') }}">Inicio</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_anime_list') }}">Explorar</a>
                        </li>
                        {% if app.user %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_library_list') }}">Bibliotecas</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_favorites') }}">Favoritos</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_dashboard') }}">Dashboard</a>
                        </li>
                        {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link text-danger" href="{{ path('app_admin_dashboard') }}"><i class="fa-solid fa-shield-halved"></i> Admin</a>
                        </li>
                        {% endif %}
                        {% endif %}
                    </ul>
                    <div class="ms-lg-4 mt-3 mt-lg-0 d-flex gap-2">
                        {% if app.user %}
                        <div class="dropdown">
                            <button class="btn btn-outline-warning btn-sm dropdown-toggle" type="button" data-bs-toggle="dropdown">
                                <i class="fa-solid fa-user"></i> {{ app.user.username }}
                            </button>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li><a class="dropdown-item" href="{{ path('app_profile') }}">Perfil</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="{{ path('app_logout') }}">Cerrar sesión</a></li>
                            </ul>
                        </div>
                        {% else %}
                        <a href="{{ path('app_register') }}" class="btn btn-outline-warning btn-sm">Registrarse</a>
                        <a href="{{ path('app_login') }}" class="btn btn-warning fw-semibold btn-sm px-3">Acceder</a>
                        {% endif %}
                    </div>
                </div>
            </nav>
        </div>
    </header>

    <main class="pb-5">
        <div class="container py-4">
            {% include '_partials/_flash_messages.html.twig' ignore missing %}
            {% block body %}{% endblock %}
        </div>
    </main>

    <footer class="py-4">
        <div class="container text-center text-white small">
            &copy; {{ 'now'|date('Y') }} OtakuNest · Donde vive tu anime
        </div>
    </footer>

    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    {% block javascripts %}{% endblock %}
</body>
</html>